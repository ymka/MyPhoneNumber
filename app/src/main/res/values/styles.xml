<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorButtonNormal">@color/colorAccent</item>
    </style>

    <style name="PhoneText">
        <item name="android:textSize">40sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/phoneText</item>
        <item name="autoSizeTextType">uniform</item>
        <item name="autoSizeStepGranularity">2sp</item>
        <item name="android:lines">1</item>
    </style>

    <style name="WidgetButtonParent">
        <item name="android:layout_gravity">center_vertical</item>
    </style>

    <style name="WidgetButton" parent="WidgetButtonParent">
        <item name="android:background">?android:selectableItemBackground</item>
    </style>

    <style name="RequestText" parent="android:TextAppearance.Material.Caption">
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="RequestPermissionText" parent="android:TextAppearance.Material.Body2">
        <item name="android:textColor">@color/selector_request_permission</item>
        <item name="android:textSize">@dimen/request_permissions_text</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/toolbarText</item>
    </style>

    <style name="ToolbarPopup" parent="ThemeOverlay.AppCompat">
        <item name="colorControlNormal">@color/colorAccent</item>
    </style>

</resources>
